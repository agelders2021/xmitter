Pre-Layout Punch List -- 2026-07-06
====================================

Deep-dive audit of the analog control board (arduino + bias + buffer_keyer
+ vfo + interface sheets) before starting PCB layout. Items are grouped by
severity. Check off as you go -- change "[ ]" to "[x]" in any text editor,
optionally with a "-- resolved" note explaining what was done.


UPDATES SINCE FIRST AUDIT
==========================

Path A executed: all hierarchical_label instances converted to global_label
across the six active sheets, and the root sheet had its now-vestigial
sheet pins, wires, junctions, and stitching labels stripped. Also renamed
three _OUT/_IN pairs to unified names so same-name globals auto-connect:

  GRID_CTL_A_OUT + GRID_CTL_A_IN -> GRID_CTL_A
  GRID_CTL_B_OUT + GRID_CTL_B_IN -> GRID_CTL_B
  RAW_RF_OUT     + RAW_RF_IN     -> RAW_RF

Every cross-sheet net now shares one name across all sheets that touch it.
Verified matrix:
  3V3               buffer_keyer, vfo
  CS_DAC            arduino, buffer_keyer
  GRID_BLOCK_CRASH  arduino, bias
  GRID_CTL_A/B      bias, buffer_keyer
  I_CATHODE_A/B     arduino, bias
  LDAC_4728         arduino, buffer_keyer
  MBL600_A/B        arduino, interface
  MOSI, SCK         arduino, buffer_keyer
  RAW_RF            buffer_keyer, vfo
  RESET_N           arduino, buffer_keyer
  SDA, SCL          arduino, buffer_keyer, interface

Consequences for this punch list:
  - Section B1 (root sheet pins + wires) is now fully resolved.
  - L1 (RESET_N linkage) is resolved -- both sheets use the same global.
  - L2 (+5V_REF root pin) is moot -- root pins are all gone.
  - GRID_CTL_A false alarm: my earlier trace flagged bias:GRID_CTL_A as
    "unwired." False positive. The label sits directly on R_PADA's left
    pin (69.85, 86.36) which is a valid KiCad connection idiom. Full
    verified path: MCP4728 U8 VC -> R_PADA -> OPA454 U5 IN+ + Q3 drain.


ERC PASS RESULTS (2026-07-06, from KiCAD/ERC.rpt)
==================================================

74 errors + 2 warnings. Categorized: 5 real bugs, ~8-10 clearable by
adding PWR_FLAG symbols, rest are known non-issues (unused pins on chips
by design, STEMMA-cabled power for the Si5351, and the entire Front_Panel
sheet which is deferred). New action items B4 and B5 below; existing
items H1, H2, H3 already cover the intent of some ERC complaints.


BLOCKERS -- must fix before layout
===================================

[x] B1a-e. Root sheet pins + stitching wires -- resolved by Path A globals.

[ ] B2. Wire J10 paddle connector on Arduino sheet:
        J10 is a 2-pin THT header that mates to a 2-conductor 1/4" phone
        plug going out to the front panel (single-contact key or single-
        lever paddle -- one signal + shield-return).

        Wire:
        - J10 pin 1 (tip)    -> Metro GPIO for key input (D5 suggested;
                                  see below)
        - J10 pin 2 (sleeve) -> GND

        Metro pin choice:
        - AVOID D4 -- reserved as encoder INT (currently at J5 coax)
        - AVOID D6 -- TX_LED is not firmware-driven (see M5)
        - AVOID A0-A4 -- ADC2 pins, WiFi conflict
        - D5 is available and unused. The library-symbol alias "D5/DAH"
          is just an Adafruit-doc naming hint; firmware treats it as a
          plain digital input with pull-up.

        Update the arduino sheet's paddle text note. Current text:
          "J10 connects to the paddle. Function of D5 and D6 can be
          swapped in software."
        Rewrite to match the actual design:
          "J10 = paddle/key input via front-panel 2-conductor 1/4-inch
          phone jack. Tip -> D5 (INPUT_PULLUP), sleeve -> GND."

[x] B3. Footprints assigned -- resolved 2026-07-07.

        Arduino sheet:
        [x] R30 (470K)  -> Resistor_THT:R_Axial_DIN0207_L6.3mm_D2.5mm_P10.16mm_Horizontal
        [x] C41 (1uF)   -> Capacitor_THT:C_Rect_L7.2mm_W4.5mm_P5.00mm
        [x] C42 (100nF) -> Capacitor_THT:C_Disc_D5.0mm_W2.5mm_P5.00mm
        [x] C43 (10uF)  -> Capacitor_THT:CP_Radial_D5.0mm_P2.00mm

        Bias sheet:
        [x] J11 (GRID_BIAS)     -> Connector_PinHeader_2.54mm:PinHeader_1x02_P2.54mm_Vertical
        [x] J12 (CATHODE_SENSE) -> same
        [x] J14 (2-pin)         -> same (role still to decide, but header
                                   footprint is a safe default)

        Buffer_keyer sheet:
        [x] J13 (5-pin)         -> Connector_PinHeader_2.54mm:PinHeader_1x05_P2.54mm_Vertical

        Interface sheet:
        [x] J15 (3-pin)         -> Connector_PinHeader_2.54mm:PinHeader_1x03_P2.54mm_Vertical

        Only U2 (Metro) remains footprint-less, intentionally off-PCB.

[ ] B4. Fix real ERC bugs (5 items):

        [x] B4a. VFO sheet: C_coup -> renamed to C_IN1 (10nF at 86.36,
                 62.23). Resolved 2026-07-07.

        [x] B4b. Duplicate R6: VFO's 270R R6 renamed to RD1. Buffer_keyer
                 keeps R6 (2K7) as the sole R6 in the project. Resolved
                 2026-07-07.

        [x] B4c. J2 pin 2 marked NC (no_connect flag at 26.67, 81.28
                 matches J2 pin 2). Resolved 2026-07-07. Note: J2's Value
                 field still says "Conn_01x02_Pin" (placeholder). Rename
                 the Value to whatever role J2 plays if it isn't going to
                 be deleted.

        [x] B4d. TP2 (MOD_RF+) -- resolved 2026-07-07. User verified in
                 KiCad: junction dots + wires connect TP2 pin to
                 MOD_RF_OUT+ net.

        [x] B4e. TP7 (MOD_RF-) -- resolved 2026-07-07. Same as B4d --
                 junction + short wire electrically connects TP7 to
                 MOD_RF_OUT-. TP3 = SCL is intentional (I2C-bus probe
                 for debug access).

[ ] B5. Add PWR_FLAG symbols to silence "power_pin_not_driven" errors.

        Currently zero PWR_FLAG components exist project-wide. Each
        externally-sourced rail needs one PWR_FLAG somewhere on the sheet
        where the rail arrives.

        [ ] B5a. buffer_keyer: PWR_FLAG on +3.3V, placed at the MCP4728
                 breakout VIN pin. STEMMA-QT cable is the physical source.
        [ ] B5b. buffer_keyer: PWR_FLAG on VEE. VEE is the negative rail
                 for MC1496 and other analog stages -- arrives externally.
        [ ] B5c. buffer_keyer: PWR_FLAG on +12V, placed at whatever
                 connector/point ends up sourcing +12V (see H2 -- the
                 decision cascades to this location).
        [ ] B5d. bias: PWR_FLAG on -90V, placed at the incoming -90V point.
                 Arrives from the separate power-supply project.
        [ ] B5e. arduino: PWR_FLAG on +12V if the source lands here (again,
                 depends on H2 decision).
        [ ] B5f. arduino: PWR_FLAG on +3.3V if the source lands here (may
                 not be needed if all +3.3V is sourced via STEMMA at
                 buffer_keyer's MCP4728 VIN -- verify after B5a).


HIGH PRIORITY -- decide before ordering PCB
============================================

[ ] H1. C2 (MAINS_HEARTBEAT / K_MAIN interlock)
        The CD14538B on Q5 protects only the ON-BOARD HV contactor driver.
        The MAINS-SIDE K_MAIN contactor (upstream of the HV supply, in the
        supply project) is not yet handed off to the analog board.

        Decide between:
        [ ] (a) Use CD14538B section B on the arduino sheet to generate a
                second hardened heartbeat that exits via a dedicated
                connector (or spare umbilical pin) to the supply project's
                K_MAIN driver. Same monostable-hardened topology on both
                HV switches.
        [ ] (b) Route a raw GPIO heartbeat via a dedicated connector to
                the supply project, and let the supply project's own
                monostable do the enforcement.

        Once decided, add the connector footprint and a global label
        on the arduino sheet.

[ ] H2. C3 (+12V rail source)
        Three coils tie to +12V (J6 T/R, J7 HV contactor, J8 HV in-rush
        bypass). No +12V source is drawn anywhere on the board.

        Decide between:
        [ ] (a) Metro Vin passthrough (puts Metro on the safety-critical
                path -- USB disconnect kills +5V too)
        [ ] (b) Dedicated +12V input connector on the analog board
                (RECOMMENDED -- cheap to add now, expensive after fab)
        [ ] (c) Local regulation from a higher rail

        If (b), add a 2-pin connector footprint to the arduino sheet with
        a +12V power symbol on pin 1 and GND on pin 2, plus a bulk cap
        (10uF or larger) and 100nF bypass right at the connector.

[ ] H3. U18 (CD14538B) VDD rail choice
        Datasheet V_IH_min at VDD=5V is 3.5V. The heartbeat trigger comes
        from PCF8575 U17 pin P12. What rail is U17 powered from?

        [ ] Confirm PCF8575 U17 supply rail (should be either +5V or +3.3V).
        [ ] If U17 is on +5V, U18 at +5V is fine -- no change needed.
        [ ] If U17 is on +3.3V, move U18 to +3.3V:
                - VDD tie: change #PWR0120 from +5V to +3.3V
                - VDD tie for reset A/B and unused-section ties: also to +3.3V
                - Q7 gate drive still fine (2N7000 Vgs(th) ~2V)

        At VDD=3V the V_IH_min drops to ~2.1V, comfortably below any
        3.3V logic HIGH.


MEDIUM -- readability / documentation
======================================

[ ] M1. Add HV_ENABLE global label on the arduino sheet along the wire from
        U17 pin P14 to Q5 gate.

[ ] M2. Add HV_HEARTBEAT global label on the arduino sheet along the wire
        from U17 pin P12 to U18 pin 4 (+TR_A).

[ ] M3. Update the stale I2C-topology text note on the arduino sheet. The
        current text reads:
             "I2C bus and +3.3V routed off-board via STEMMA QT cable from
              Metro JST-SH port to Si5351 breakout (VFO sheet). Si5351
              breakout 0.1" header distributes SDA/SCL/+3V3/GND to other
              I2C devices ..."
        Rewrite for the new topology:
             Metro STEMMA-QT --> MCP4728 breakout --> MCP4728 0.1" header
             carries SDA/SCL/+3V3/GND to the PCB traces (reaches U17 and
             the RJ45 pins). Si5351 breakout is CHAINED off MCP4728's
             STEMMA-OUT port, installed AFTER the one-time EEPROM reprogram.

[ ] M4. 3V3 vs +3.3V naming: buffer_keyer and VFO use the global label
        "3V3", arduino uses power symbol "+3.3V". These are still separate
        nets after Path A.
        [ ] Decide: unify by dropping the 3V3 global label on both sheets
            and replacing with the "+3.3V" power symbol, OR document that
            they are intentionally isolated power domains (STEMMA-cabled
            Si5351 as its own domain).

[x] M5. TX_LED -- resolved by design decision (not firmware-driven).
        The front-panel TX LED will be wired in parallel with the T/R
        relay coil (with an appropriate series R for the coil voltage).
        When firmware energizes T/R, the coil pulls in and the LED
        lights automatically. No Metro GPIO needed.
        The "D6/TX_LED" alias in the Metro library symbol is just an
        Adafruit-doc naming hint; D6 remains available for future use.

[ ] M6. (Cosmetic) On the bias sheet, GRID_CTL_A uses the label-at-pin-origin
        idiom (label directly on R_PADA left pin, no visible wire).
        GRID_CTL_B uses a short wire from the label to R_PADB. Both work,
        but GRID_CTL_A silently breaks if R_PADA ever moves. Consider
        adding a short wire to match the GRID_CTL_B style for robustness.


LOW -- deferred but worth noting
=================================

[x] L1. RESET_N -- resolved by Path A. Both sheets share the global.

[x] L2. +5V_REF -- resolved by Path A. No root sheet pins exist anymore;
        the label is local to bias only, which is fine.

[ ] L3. Verify custom footprint physical dimensions (from pcb_fab_checklist):
        [ ] Amphenol RJE1D-188 (RJ45) -- 5 items already flagged
        [ ] Adafruit PCF8575 breakout -- Y-offset
        [ ] Adafruit MCP4728 breakout -- silk-label to pad-number map
        [ ] SameSky RCJ-02 RCA jack -- pin locations
        [ ] Adafruit Si5351A STEMMA -- confirm
        [ ] Adafruit_MCP4728_Breakout has "VERIFY silk-label to pad-number
            correspondence" flag in its descr -- resolve.


CONFIRMED SOUND -- no action needed
====================================

- C42 fix confirmed: sits between +5V and GND at U18 with C43 (10uF) bulk
- Q5-Q7 series stack topology correct
- CD14538B RC timing (R30 470K + C41 1uF -> 470 ms)
- CD14538B power ties, reset A/B, unused-section ties
- All I_CATHODE and MBL600 signal chains within their sheets
- Bias sheet GRID_CTL_A/B chain: MCP4728 U8 VC/VD -> R_PADA/B (1K padding)
  -> OPA454 U5/U10 IN+ -> Q3/Q2 drain (crash slam). Verified.
- All hierarchical labels within the buffer_keyer, vfo, and interface
  sheets are internally consistent
- External flyback diodes at each relay coil noted in text -- planned for
  supply project side
- Text-note cleanup on arduino sheet already done (no stale J8 or J50
  references)


ERC NON-ISSUES (categorized once, so we don't re-check next run)
================================================================

After B4 and B5 are done, the remaining ~55 ERC errors are all known,
expected non-issues. You can right-click each in the KiCad ERC panel ->
Exclude to hide them from future runs.

Category A -- Unused chip pins by design:
  - U2 A0-A4 (Metro ADC2 pins; unused because of WiFi conflict)  ...5
  - U2 AREF (unused ADC reference)                              ...1
  - U18 pin 7 ~Q_A (we only use Q_A, not the inverted output)    ...1
  - U18 pins 9, 10, 14, 15 (unused CD14538B section B)           ...4
  - U17 P0-P11 + IRQ (unused main-board PCF8575 expander pins)   ...9
  - U17 A0-A2 (hardware jumpers on the Adafruit breakout; the
    schematic pins remain NC but the breakout handles the tie
    physically) -- clears after B5a adds PWR_FLAG on +3.3V       ...6
  - U1 CLK1, CLK2 (Si5351 only uses CLK0 for the 14 MHz VFO)     ...2

Category B -- STEMMA-QT cable is off-schematic:
  - U1 VIN, GND on VFO sheet (Si5351 gets +3.3V and GND via the
    STEMMA cable chained from MCP4728; no PCB trace exists)      ...2

Category C -- Deferred sheet (populate in Phase 3+):
  - Entire Front_Panel sheet: U15 PCF8575 all pins              ...28

Category D -- Stub sheets (Phase 3-5 destinations):
  - MOD_RF_OUT+ / MOD_RF_OUT- "isolated_pin_label" warnings --
    destination Driver / PA sheets are empty stubs               ...2

Category E -- Pin-type quirk in the Metro symbol:
  - U2 5V pin flagged as "power_output" collides with U9 (LM7805)
    VO pin also typed "power_output." In practice the Metro's 5V
    pin can accept external 5V input; ERC just sees both as sources.
    Either add an ERC directive on this violation or change the
    Metro symbol's 5V pin type to power_input.                    ...1


RECOMMENDED WORK ORDER
=======================

1. B4 (real ERC bugs) -- annotate to fix C_coup + R6 dup, wire TP2/TP7,
   decide J2. About 5 min in KiCad.
2. B5 (PWR_FLAG symbols) -- 4-6 flags total. About 5 min.
3. Rerun ERC -- should drop from 74 errors + 2 warnings to about 55
   errors, all in known-non-issue categories above. Right-click ->
   Exclude to hide them.
4. B2 (paddle wiring) -- small hands-on.
5. H1 + H2 decisions, then add connectors -- one edit pass. H2 decision
   also unblocks placing the +12V PWR_FLAG (B5c/B5e).
6. H3 (PCF8575 supply verification) -- read U17 datasheet + edit if needed.
7. B3 (footprint assignments) -- can partly run assign_cap_footprints.py.
8. M1, M2, M3, M6 (labels + text note + wire) -- cosmetic.
9. M4 (3V3 vs +3.3V) decision -- may cascade to a small Path A cleanup.
10. L3 (custom footprint physical check) -- when parts arrive.


VERDICT
========

ERC identified only 5 real bugs (B4) and a systemic missing-PWR_FLAG
pattern (B5). No topology surprises. BLOCKERS is longer than yesterday
by two items (B4, B5) but both are quick fixes. Everything else still
holds: ready to move to layout once B2/B3/B4/B5 are done and H1/H2/H3
are decided; nothing found warrants a schematic redesign.
