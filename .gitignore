# QUCS-S regenerated outputs
*.ngspice
*.cir
*.dat.ngspice
spice4qucs.*.plot
# Hand-written source decks (NOT QUCS-regenerated) — track these
!xmitter_prj/vfo_lpf_ac.cir

# LTspice intermediate output
*.raw
*.op.raw
*.log

# Backup files
*.bak
*.~*~

# Emacs backup and auto-save files
*~
\#*\#

# Python
__pycache__/
*.pyc

# Editor / OS
.vscode/
Thumbs.db
~$*

# KiCad
.history/
~*.lck
*.kicad_pcb-bak
*.kicad_sch-bak
# Per-user editor state (open tabs, zoom, active layer) — not design content
*.kicad_prl
fp-info-cache
# KiCad generated reports (ERC, Update-PCB-from-Schematic diff, DRC, etc.)
KiCAD/ERC.rpt
KiCAD/report.txt
KiCAD/DRC.rpt
# Same reports in per-board subdirectories
KiCAD/**/ERC.rpt
KiCAD/**/DRC.rpt
KiCAD/**/report.txt
# Gerber fabrication outputs -- regenerable from the .kicad_pcb source
KiCAD/**/gerber/
KiCAD/**/gerber.zip
# KiCad ad-hoc plot output PDFs (redundant with gerbers; regenerable)
KiCAD/KiCAD-*.pdf
# Stray plot PDFs at repo root (accidental output path)
/KiCAD-*.pdf

# Claude Code per-machine personal overrides
.claude/settings.local.json
